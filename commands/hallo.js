const discord = require("discord.js");

module.exports.run = async (client, message, args) => {

    return message.reply(`Hallo (Doe !hgh om verder te praten!)`);

}

module.exports.help = {
    name: "hallo"
}