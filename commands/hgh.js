const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    return message.reply(`Goed met jou! (Doe !og om verder te praten!)`);

}
module.exports.help = {
    name: "hgh"
}